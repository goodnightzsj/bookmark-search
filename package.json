{
  "name": "bookmark-search",
  "version": "1.9.0",
  "description": "Lightning-fast bookmark manager and search tool for Chrome",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild-check.mjs",
    "preview": "vite preview",
    "test": "node --test",
    "postbuild-check": "node scripts/postbuild-check.mjs",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "terser": "^5.44.1",
    "typescript": "^5.6.3",
    "vite": "^5.0.0",
    "vite-plugin-static-copy": "^1.0.0"
  }
}
